server:
  port: 8090
spring:
  datasource:
    # Use standard environment variables with defaults for local testing
    #url: ${DB_URL:jdbc:postgresql://localhost:5432/stock}
    #url: ${DB_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres}
    url: ${DB_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres}

    #username: ${DB_USERNAME:postgres}
    username: ${DB_USERNAME:postgres.tymsvhihcwwzjkrfkyol}
    #password: ${DB_PASSWORD:postgres}
    password: ${DB_PASSWORD:supabase.gestion}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # Automatically updates schema based on your entities
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
cors:
  allowed:
    origin: ${CORS_URL:*}
    # origin: ${CORS_URL:http://localhost:4200}
logging:
  file:
    name: logs/application.json
  structured:
    format:
      console: ecs
      file: ecs